# Stage_2 - Post-downsampling workflow.
include: "rules/common.smk"

# Set new sample list based on filtered QC from stage 1
PROJECT = config["project_name"]
BIN = config["bins"]

prplpu = "FALSE"
if all(samplesheet.precPloidy.notna()) and all(samplesheet.precPurity.notna()):
    prplpu = "TRUE"

SAMPLES = get_ds_bams(PROJECT, BIN)


localrules:
    all,
    ds_symlink,


rule all:
    input:
        expand(
            OUT_DIR
            + "sWGS_fitting/{project}_{bin}kb/absolute_POST_down_sampling/{project}_{bin}kb_ds_abs_fits.tsv",
            project=config["project_name"],
            bin=config["bins"],
        ),


# pipeline rules
include: "rules/split_input.smk"
include: "rules/ds_symlink.smk"
include: "rules/downsample.smk"
include: "rules/downsampled_rel_rds.smk"
include: "rules/rel_to_abs.smk"
include: "rules/combine_results.smk"
